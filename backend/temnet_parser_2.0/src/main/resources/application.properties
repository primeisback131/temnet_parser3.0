# Database connection. Override via environment variables (do NOT commit real secrets).
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/ejabberd}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# HikariCP connection pool (Spring Boot default pool).
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.pool-name=temnet-pool

# Analytics DB owned by this app (normalized messages + tickets). Created
# automatically; the sync job fills it from the ejabberd dump.
app.analytics.url=${ANALYTICS_DB_URL:jdbc:mariadb://localhost:3306/temnet_analytics?createDatabaseIfNotExist=true}
app.analytics.username=${ANALYTICS_DB_USER:root}
app.analytics.password=${ANALYTICS_DB_PASSWORD:root}

# Dump -> analytics sync cadence (ISO-8601 durations).
app.sync.initial-delay=${SYNC_INITIAL_DELAY:PT30S}
app.sync.interval=${SYNC_INTERVAL:PT5M}

# Jids starting with this prefix are support operators.
app.operator-prefix=${OPERATOR_PREFIX:help}

# LLM classification of ambiguous reopen candidates (empty key = disabled).
# Verdicts are cached in llm_verdict, so rebuilds never re-pay for old cases.
# Calls are paced to the Anthropic free-tier limit (5 requests/minute), and
# max-per-sync is sized so one sync's LLM work (20 calls * 12s) fits inside
# the 5-minute sync interval; the remaining pending cases drain on later runs.
app.llm.api-key=${ANTHROPIC_API_KEY:}
app.llm.model=${LLM_MODEL:claude-haiku-4-5}
app.llm.max-per-sync=${LLM_MAX_PER_SYNC:20}
app.llm.requests-per-minute=${LLM_RPM:5}

# CORS: allowed origin of the frontend (Vite dev server by default).
app.cors.allowed-origin=${CORS_ORIGIN:http://localhost:5173}

# Metrics response cache. The metric queries are heavy window-function scans
# over the archive, and historical data never changes - only "today" does, so
# a short TTL keeps the dashboard snappy without showing stale numbers.
spring.cache.type=caffeine
spring.cache.cache-names=timeseries,heatmap,sla,resolution,reopens,alerts,categories,operators
spring.cache.caffeine.spec=expireAfterWrite=${METRICS_CACHE_TTL:10m},maximumSize=500
